export interface optionsType {
  winners: string[];
  shuffleResultShow: string;
  numberOfWinners: number;
  namesCount: number;
  finished: boolean;
  modal: boolean;
  filter: boolean;
  remove: boolean;
  nameArray: string[];
  text: string;
}
